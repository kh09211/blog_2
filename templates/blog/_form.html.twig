{{ form_start(form) }}
    {{ form_widget(form) }}
    <div class="flex row justify-content-center">

        <button class="d-block btn btn-primary my-2 mx-1">{{ button_label|default('Save') }}</button>
        
        {# THE DELETE FORM HOWEVER THIS WILL NEED ROUTE MODIFICATION TO WORK CORRECTLY NESTED FORM
        {% if (blog.title != null) %}
        {{ include('blog/_delete_form.html.twig') }}
        {% endif %}
        #}

    </div>
{{ form_end(form) }}
